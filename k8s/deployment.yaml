apiVersion: apps/v1
kind: Deployment
metadata:
  name: contract-lifecycle-crew
  labels:
    app: contract-lifecycle-crew
    project: ai-engineer-portfolio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: contract-lifecycle-crew
  template:
    metadata:
      labels:
        app: contract-lifecycle-crew
    spec:
      containers:
        - name: contract-lifecycle-crew
          image: contract-lifecycle-crew:latest
          ports:
            - containerPort: 8000
              protocol: TCP
          env:
            - name: ENVIRONMENT
              value: "production"
            - name: LOG_LEVEL
              value: "INFO"
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: contract-lifecycle-secrets
                  key: openai-api-key
                  optional: true
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: contract-lifecycle-secrets
                  key: anthropic-api-key
                  optional: true
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: contract-lifecycle-crew
  labels:
    app: contract-lifecycle-crew
spec:
  type: ClusterIP
  ports:
    - port: 8014
      targetPort: 8000
      protocol: TCP
  selector:
    app: contract-lifecycle-crew
